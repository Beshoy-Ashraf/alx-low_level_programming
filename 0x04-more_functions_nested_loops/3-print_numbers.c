#include "main.h"
/**
 * print_numbers - function that prints 0 to 9
 * Return: 0 (Success)
 */
void print_numbers(void)
{
	int letter;

	for (letter = 48; letter <= 57; letter++)
	{
		_putchar(letter);
	}
	_putchar(10);

}
